/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['dashboard.clerk.com', 'img.clerk.com'], // Add other domains as needed
      },
};

export default nextConfig;
